<h2>Témata</h2>
<p>Vyberte si jaké téma, respektive oblast přírody se chcete začít učit a stiskněte <strong>Řešit</strong>.</p>

{{#if invalid}}
	<div class="flash error">Omlouváme se, ale toto téma je momentálně neprovozuschopné. Zkuste to, prosím, později.</div>
{{/if}}

{{#if interruption}}
	<div class="flash info">Zkuste si vybrat jiné téma :-)</div>
{{/if}}

{{#each category in model}}
	<div class="group">
		{{#if others}}
			<h2>Ostatní</h2>
		{{/if}}
		<h2>{{category.name}}</h2>
		{{#each concept in category.concepts}}
			<div class="concept">
				{{#link-to 'play' concept.id_concept classNames="start-button"}}Řešit{{/link-to}}

				<span class="concept-name">{{concept.name}}</span>
				<span class="concept-description">{{concept.description}}</span>
				<div class="cleaner"></div>
			</div>
		{{/each}}
	</div>
{{/each}}