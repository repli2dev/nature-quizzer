<h2>{{translate 'facebook.heading'}}</h2>
<p>{{translate 'facebook.description'}}</p>

{{#if isTheirFailure}}
	<div class="flash error">{{translate 'facebook.their'}}</div>
{{/if}}

{{#if isUnknownFailure}}
	<div class="flash error">{{translate 'facebook.unknown'}}</div>
{{/if}}

{{#if isRegistrationFailure}}
	<div class="flash error">{{translate 'facebook.registration'}}</div>
{{/if}}

{{#if isUnavailability}}
	<div class="flash error">{{translate 'facebook.unavailable'}}</div>
{{/if}}